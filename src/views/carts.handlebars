<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/products">Total eSports</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="/products">Productos</a>
        {{#if isAdmin}}
        <a class="nav-link" href="/realTimeProducts">Administrar productos</a>
        <a class="nav-link" href="/users">Administrar Usuarios</a>
        {{/if}}
        {{#if isPremium}}
        <a class="nav-link" href="/realTimeProducts">Administrar productos</a>        
        {{/if}}        
      </div> 
      <div class="d-flex align-items-center mx-2"> 
        <a href="/profile"><i class="fa-solid fa-user"></i></a>
      </div>
      <div class="d-flex align-items-center mx-2"> 
        <a href="/chat"><i class="fa-solid fa-comments"></i></a>
      </div>     
    </div>    
  </div>  
  <span class="ml-auto"><a href="" id="cartId" data-cart-id="{{user.cartId}}"></a>
  <i class="fas fa-shopping-cart"></i>
</a></span>  
</nav>

<h2 class="text-center">Detalle del carrito</h2> <button id="clearCartButton" class="btn btn-danger mt-2" data-cart-id={{id}}>Vaciar carrito</button>

<p id="cartId" data-cart-id="{{user.cartId}}">ID del carrito: {{id}}</p>
<ul class="list-group">
  {{#each productos}}
    <li class="list-group-item d-flex justify-content-between align-items-center">
      {{this.quantity}} {{this.name}} - $ {{this.price}}
      <span>
        <a href="#" class="delete-product" data-product-id="{{this.idProducto}}">
        <i class="fa-solid fa-trash-can"></i>
      </a>       
      </span>
    </li>
  {{/each}}
</ul>
<button id="purchaseButton" class="btn btn-primary mt-2" data-cart-id={{id}}>Finalizar compra</button>

<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/purchase.js"></script>
