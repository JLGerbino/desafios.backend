import ProductManager from "./manager/productManager.js";

const manager = new ProductManager();

const env = async() =>{    
    
    //let carga = await manager.addProduct("Cama", "Colchon 140 x 190", 82900,"no imagen", 20, 20);
    
    //let productos = await manager.getProducts(); console.log(productos)
    
    //let id = await manager.getProductsById(3);
    
    //let borrar = await manager.deleteProduct(4); console.log(borrar)

    //let modificar = await manager.updateProduct(3, "Mesa tv","casa puerta", 10, "si imagen",3,10); console.log(modificar)
}

env()